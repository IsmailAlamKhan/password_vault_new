targets:
  $default:
    builders:
      freezed:freezed:
        generate_for:
          include:
            - lib/**_codegen.dart
      json_serializable:json_serializable:
        generate_for:
          include:
            - lib/**_codegen.dart
